# Python Tutor - Backend (do not commit .env or real keys)

# Groq API key (free tier at https://console.groq.com). Used for grading when students log in or when no key is sent.
GROQ_API_KEY=

# Optional: OpenAI instead of Groq
# OPENAI_API_KEY=

# Optional: Allow students to use your API key by logging in. JSON array of {username, password_hash}.
# Generate hashes with: python hash_password.py myuser mypassword
# ALLOWED_USERS=[{"username":"student1","password_hash":"$2b$12$..."}]
